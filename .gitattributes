# データファイルは改行コードを変換しない。
#
# 県から取得したCSVはCRLF。Gitの自動変換に任せると、
# Windowsで作業した内容とLinux（GitHub Actions）で取得した内容の
# 改行が食い違い、中身が同じでも「変更あり」と判定されてしまう。
# 毎日の自動更新で、変わっていないのに毎日コミットされることになる。
*.csv -text

# 文字コードの判定を助ける（すべてUTF-8）
*.csv diff
