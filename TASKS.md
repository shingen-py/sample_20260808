# TASKS.md — 次に何をするか

この文書は作業の現在地です。`PROJECT.md`と`DATA.md`を確定した後、AIに案を作らせ、人間が確認してからタスク表へ反映します。

**ZIPを開いただけでは、あなたのMVP用タスクは自動生成されません。**

## 現在の状態

- `PROJECT.md`: 確定（UI改善フェーズの完成条件を追加済み）
- `DATA.md`: 確定（ライセンスは2026年8月7日に人間が確認済み。更新頻度のみ要確認）
- タスク案: 人間が確認して反映済み
- MVP本体: T-000〜T-010がすべて`done`
- UI改善フェーズ（U-001〜U-015）は`done`。U-016は修正済みで人間の再確認待ち
- 目撃リスト（L-001〜L-006）を追加した。`PROJECT.md`の完成条件にも2項目を足した
- 目撃リスト（L-001〜L-007）はすべて`done`。人間の画面確認は後日
- 公開の準備（G-001〜G-004、G-007）を追加した。**公開そのものは行わない**
- 公開の準備（G-001〜G-004、G-007）はすべて`done`。**公開の操作は行っていない**
- 免責・プライバシー・連絡先（P-001〜P-004）はすべて`done`
- **公開した**（2026年8月8日）。GitHub: https://github.com/shingen-py/sample_20260808 （public）。毎日の自動更新が動くことを確認済み
- 次にすること: `G-006` Streamlit Community Cloudへデプロイ（人間の作業）

## TASKS.mdを作る手順

1. `PROJECT.md`を確定する
2. `DATA.md`を確定する
3. AIに「TASKS.mdの案を作る」を依頼する
4. 人間が順番・範囲・完成条件を確認する
5. AIに「この案を反映してください」と依頼する
6. 最初の1件だけを`doing`にする

## 状態の意味

- `todo`: まだ始めていない
- `doing`: 今取り組んでいる。常に1件まで
- `done`: 完成条件を確認した
- `blocked`: 人間の判断や環境の準備が必要

## タスク一覧

| ID | タスク | 完成条件 | 担当 | 状態 | 目安 |
|---|---|---|---|---|---|
| T-000 | 利用条件・個人情報の確認 | 自然共生推進課またはデータプラットフォームの利用規約でライセンスを確認し、`DATA.md`第6節の2つのチェックが埋まる | 人間 | done | 2026年8月7日に人間が確認 |
| T-001 | 実データCSVを`data/`に置く | `data/2026kumadata.csv`（111件）と`data/2026kumadata_new.csv`（49件）が存在し、文字コードが判明して`DATA.md`の該当欄が確定する | 人間+AI | done | 10分 |
| T-002 | テスト用の小さなCSVを作る | `tests/data/`に5〜10件のCSVがあり、座標が空欄の行・市町村が空欄の行を意図的に各1件含む | AI | done | 10分 |
| T-003 | `data_utils.py`の読み込みを差し替える | 2ファイルを結合して160件読める。必須9列が欠けると`ValueError`。テストが通る | AI | done | 30分 |
| T-004 | 座標の分離と市町村の絞り込み | 座標なしの行を分離して件数が取れる。市町村の一覧が作れ、選んだ市町村で絞れる。空欄は「市町村不明」扱いで一覧に出ない。テストが通る | AI | done | 30分 |
| T-005 | 地図ライブラリを導入する | `requirements.txt`に`folium`と`streamlit-folium`を追加し、インストール後に`import`が通る | AI | done | 10分 |
| T-006 | 画面を地図に作り替える | 市町村のプルダウンと地図が表示され、市町村を選ぶとピンと件数が変わる | AI | done | 30分 |
| T-007 | ピンの吹き出しに詳細を出す | ピンをクリックすると`年月日` `時間` `場所` `目撃時のクマ` `人身被害の有無`の5項目が読める | AI | done | 20分 |
| T-008 | 0件・座標なし・出所表示 | 0件のとき収録期間つきの案内が出る。座標なしN件の注記が出る。出所文言と「位置は目安」の但し書きが常時見える | AI | done | 20分 |
| T-009 | テストとREADME、1分デモ | `python -m unittest discover -s tests -v`が全て通る。READMEが新MVPの説明になり、1分デモの手順が書かれている | AI | done | 20分 |
| T-010 | 画面確認で見つかった2件を直す | ピンのアイコンがリンク切れにならない。マウスホイールで意図しない拡大縮小が起きない | AI | done | 人間が画面で再確認済み |

T-000〜T-010でMVPは完成した。人間が画面を確認済み。

## タスク一覧（UI改善フェーズ）

`yamanashi_bear_map_ui_redesign.md`の指示にもとづく。既存のデータ取得ロジックと座標データは変更しない。

### P0 — 見た目の刷新

| ID | タスク | 完成条件 | 担当 | 状態 | 目安 |
|---|---|---|---|---|---|
| U-001 | デザイントークンとレイアウト土台 | `layout="wide"`にし、Light/NatureのCSS変数（色・radius・spacing）を注入。中央固定幅を解除。1440px幅で地図が横方向の70%以上を占める | AI | done | 30分 |
| U-002 | ヘッダー | 左に足跡SVGとタイトル22px、右に「2026/08/05 更新」。巨大なH1と常時表示の説明文を廃止する | AI | done | 20分 |
| U-003 | サイドバーの情報設計 | 市町村セレクトに件数を併記し高さ44px・角丸。件数を大きな数字と収録期間で表示。座標なし4件を黄色の警告から小さな注記へ降格 | AI | done | 30分 |
| U-004 | 地図タイルをmutedに | CartoDB positron系へ変更。attributionと出所表示が残っている | AI | done | 10分 |
| U-005 | SVGマーカーへ置き換え | `CircleMarker`を完全撤去。`DivIcon`とインラインSVGの涙型ピンに足跡。emojiは使わない。通常32〜36px、hover 38〜40px | AI | done | 30分 |
| U-006 | クラスタリング | `folium.plugins.MarkerCluster`を導入し、small/medium/largeをデザイン言語に合わせて上書き。クリックでズームまたはspiderfy | AI | done | 20分 |
| U-007 | Popupのカード化とLeaflet UIの上書き | 地図オブジェクトのヘッダーへCSSを注入する。popup幅280〜320px、radius 14px、shadow、閉じるボタン32px。ズームボタンも上書き | AI | done | 30分 |
| U-008 | レスポンシブ | 375px幅で横スクロールが発生しない。タブレットでサイドバー260px相当 | AI | done | 30分 |

### P1 — UX改善

データ処理と画面変更を分けるため、日付のロジックを先に片付ける。

| ID | タスク | 完成条件 | 担当 | 状態 | 目安 |
|---|---|---|---|---|---|
| U-009 | 日付ロジックを`data_utils.py`に追加 | 基準日からの経過日数、4段階の区分、期間での絞り込みが動く。テストが通る | AI | done | 30分 |
| U-010 | 期間フィルターUI | サイドバーで7日・30日・今季・すべてを選べ、件数と地図が変わる | AI | done | 20分 |
| U-011 | マーカーの色分けと凡例 | terracotta / amber / moss / slateの4色。サイドバーに凡例。色だけに意味を持たせず、ツールチップに「N日前の目撃」を出す | AI | done | 30分 |
| U-012 | 地図上のフローティング操作 | 右下に+と-と「山梨全体へ戻る」。左上に件数チップと選択中の市町村チップ | AI | done | 20分 |
| U-013 | 市町村選択時の`fitBounds` | 「平均中心と固定ズーム11」をやめ、対象地点が自然に収まる | AI | done | 20分 |
| U-014 | アクセシビリティ仕上げ | タップ領域44px以上、`prefers-reduced-motion`対応、focus-visible、コントラスト確認 | AI | done | 20分 |
| U-015 | テストとREADME更新 | 全テストが通り、READMEとデモ台本が新しいUIに合っている | AI | done | 20分 |
| U-016 | ダークテーマで期間の文字が読めない | テーマを固定し、文字色を指定した場所には背景も指定する。期間のラジオが読める | AI | done | 人間が画面で再確認済み |

## タスク一覧（目撃リスト）

地図に出ている目撃を、日付の新しい順で地図の上に一覧表示する。

| ID | タスク | 完成条件 | 担当 | 状態 | 目安 |
|---|---|---|---|---|---|
| L-001 | 並び順を`data_utils.py`に追加 | 日付の降順に並ぶ。同じ日付でも順番が毎回同じになる。日付が読めない行は末尾。テストが通る | AI | done | 20分 |
| L-002 | リストのHTMLとCSS | `ui_styles.py`に一覧を組み立てる関数を追加。各行に日付・経過・市町村・場所。見出しに件数。表示中の全件を出し、途中で打ち切らない。自由記述はエスケープする | AI | done | 30分 |
| L-003 | 地図の上に載せる | Leafletのコントロールとして右上に置く。見出しを押すと開閉できる。リスト上のスクロールとクリックが地図に伝わらない | AI | done | 30分 |
| L-004 | 行と地点を結びつける | 行をクリックするとその地点へ寄り、吹き出しが開く。クラスタの中のピンも`zoomToShowLayer`で展開してから開く | AI | done | 30分 |
| L-007 | 地図上の操作が画面に出ない不具合を直す | 一覧・チップ・「山梨全体へ戻る」が実際の画面に出る。`st_folium`が送る文字列に入っていることをテストで確認する | AI | done | 40分 |
| L-005 | 狭い画面とアクセシビリティ | 767px以下では既定で閉じる。行の高さ44px以上。キーボードで移動でき`focus-visible`が出る。コントラストが基準を満たす | AI | done | 30分 |
| L-006 | テストとREADME更新 | 全テストが通り、READMEとデモ台本に一覧の説明が入っている | AI | done | 20分 |

合計は約160分。

## タスク一覧（公開の準備）

**公開はしない。** 公開したくなったときにすぐ進められる状態までを作る。
GitHubへのpush、Streamlit Community Cloudへのデプロイ、リポジトリの公開設定は行わない。

| ID | タスク | 完成条件 | 担当 | 状態 | 目安 |
|---|---|---|---|---|---|
| G-001 | Gitリポジトリを作る | `git init`して最初のコミットができている。`.venv`と`__pycache__`が含まれず、`data/`のCSVと`.streamlit/config.toml`は含まれる | 人間+AI | done | 20分 |
| G-002 | 取得日と件数のベタ書きをやめる | CSVを差し替えると、画面の取得日・件数・収録期間が自動で変わる。`app.py`に`from __future__ import annotations`を足す。テストが通る | AI | done | 40分 |
| G-003 | CSVを取得するスクリプトを作る | `scripts/fetch_data.py`が2つのCSVを取得し、必須列・件数・文字コードを確かめてから差し替える。取得日を`data/fetched_at.txt`へ書く。壊れていたら差し替えずに終了コードで知らせる。テストが通る | AI | done | 40分 |
| G-004 | GitHub Actionsのワークフローを置く | `.github/workflows/update-data.yml`がある。毎日実行と手動実行の両方を書く。差分があるときだけコミットする。**動作確認はGitHubへ置いてからになるので、この時点では内容の確認まで** | AI | done | 30分 |
| G-007 | READMEと文書の更新 | 公開するときの手順、やめるときの手順、更新の仕組みがREADMEに書かれている。`DATA.md`の件数と取得日を直す | AI | done | 30分 |

AI担当は約140分。

## タスク一覧（免責・プライバシー・連絡先）

公開したときに必要になる表示を、先に画面へ入れておく。**公開の操作は行わない。**

置き場所の方針: **免責の要点は何も開かずに読める位置**（画面下）に出す。詳しい内容とプライバシー・連絡先は「この地図について」にまとめる。
安全に関わる注意を、開かないと読めない場所だけに置かない。

| ID | タスク | 完成条件 | 担当 | 状態 | 目安 |
|---|---|---|---|---|---|
| P-001 | 免責の要点を常時表示に足す | 画面下に「この情報に頼った結果の責任を負わない」「最新ではない（1日1回更新）」「緊急時は県・市町村の公式発表を確認」が、何も開かずに読める。テストが通る | AI | done | 20分 |
| P-002 | 「この地図について」に3つの節を足す | 免責（詳しい版）、プライバシー、連絡先の節がある。プライバシーには**外部へ通信する5つの相手を名前で書く**。連絡先はGitHubのIssuesへ誘導する。テストが通る | AI | done | 30分 |
| P-003 | 外部通信先の一覧がずれないようにする | 画面に書いた通信先の一覧と、実際に生成されるHTMLの通信先が一致することをテストで確かめる。増減したらテストが落ちる | AI | done | 20分 |
| P-004 | 公開手順に連絡先の設定を足す | READMEの「公開するときの手順」に、連絡先URLを設定する手順がある。未設定のまま公開できないことが分かる | AI | done | 10分 |

合計は約80分。

### この作業で守ること

- 免責を「この地図について」の中だけに置かない。開かなくても読める場所に要点を出す
- プライバシーに書く通信先は、実際の生成物から確かめる。手で書いた一覧が古くなると、書いてあることが嘘になる
- 連絡先URLは未確定。設定できる形にしておき、未設定のときは画面にその旨を出す
- 集めていない情報を「集めている」と書かない。逆も書かない。アクセス解析は入れない

### 今回の範囲に入れないもの

- **削除依頼への対応の仕組み**（特定の行を除外する機能）。方針だけ決めればよく、機能は未実装。必要になったら別タスクにする
- 利用規約の作成。無料・非営利で、利用者から情報を集めていないため
- アクセス解析の導入

### 公開したくなったときに行うこと（今回はやらない）

| ID | タスク | 完成条件 | 担当 | 状態 |
|---|---|---|---|---|
| G-005 | GitHubへpushして公開する | リポジトリがGitHubにあり、Actionsが手動実行で成功する | 人間+AI | done |
| G-006 | Streamlit Community Cloudへデプロイ | URLでアプリが開き、出所表示・利用条件・「山梨県が作成したものではありません」が見える | 人間 | blocked |

この2件を行う前に、人間が次を判断する。

- リポジトリをpublicにしてよいか（無料枠の前提）
- `状況`欄の記述を自分のURLで配信してよいか。`No.152`は地内名・市道番号・自宅の方角を含み、世帯が絞られうる。県が公開しているものではあるが、配信元が変わる

### この準備で守ること

- 公開の操作はしない。`git push`、デプロイ、リポジトリの公開設定は人間の指示があるまで行わない
- 取得に失敗した日は、前回のCSVを残す。空や壊れたデータで上書きしない
- 取得日・件数・収録期間を画面へベタ書きしない。データファイルから決める
- cronはUTCで書く。日本時間の朝6時なら`0 21 * * *`
- publicリポジトリは60日間活動がないと定期実行が止まる。READMEに再開の方法を書く

### この機能で守ること

- 表示中の全件を出す。多いからといって黙って打ち切らない。もし打ち切るなら画面に件数を出す
- 一覧の中身は地図に出ているピンと必ず一致させる。期間や市町村を変えたら一覧も変わる
- 行のクリックはiframeの中で完結させる。Streamlitの再実行を挟むと反応が遅くなる
- 並び順の切り替えUIは作らない。依頼は「既定では日付の降順」なので既定の並びだけにする

合計は約360分（6時間）。もくもく会2回分に相当する。1回で終わらない前提で、P0を先に完成させる。

## 実装しない項目

指示書にあるが、Streamlitの構造上できない、または今回の範囲外のもの。

| 指示書の項目 | 理由と代替 |
|---|---|
| Bottom Sheet | Streamlitの標準機能になく、実装するとフレームワークを外れる。Streamlitの折りたたみサイドバーで代替する |
| 地図をviewport全高 | Streamlitの要素構造上、厳密には不可。近似はU-001で行う |
| Lighthouse Accessibilityの完全対応 | Streamlit自体が出力するDOMを制御しきれない。指示書§18の必須項目はU-014で対応する |
| P2の全項目 | 一覧表示、URLへの絞り込み状態の保存、現在地周辺、時系列スライダー、ヒートマップ、PWA |

## UI改善フェーズで守ること

- 既存のデータ取得ロジックと座標データは変更しない
- マーカーの色は「危険度」ではなく「目撃からの経過日数」を表す。危険度は算出しない
- 経過日数の基準日は、データ内の最新日（2026年8月5日）とする。画面にその旨を明記する
- マーカーにemojiを使わない
- 真っ赤な丸マーカーへ戻さない
- attributionと出所表示を消さない
- Popupに全情報を詰め込まない

## AIが提案するときの基本構成

必要なものだけを、1件30分以内を目安に提案します。

1. 新しい小さなデータを用意する
2. CSVを読み込む処理とテストを変更する
3. 主要な絞り込み・検索・計算を実装する
4. 画面に結果と出所を表示する
5. 0件・列不足・読込失敗を扱う
6. 自動テストと人間の画面確認を行う
7. READMEと1分デモを整える

3時間で終わらない追加機能は、タスク表へ入れません。

## 今回の作業メモ

- 日時: 2026年8月7日
- いま行っているタスク: なし（`G-006`のデプロイは人間の作業）
- 分かったこと:
  - 県のPDFとは別に、やまなしデータプラットフォームにCSVがある（`kuma1`が令和8年度、`kuma0`が直近1か月）
  - 2つのCSVは列構成が同一（17列）で、`No.`が1〜111と112〜160に分かれ重複しない
  - 緯度・経度はあるが、`注意事項`列に「目撃地点の大まかな付近を表示した目安」と明記されている
  - カタログ側にライセンス表示がないため、人間の確認待ち（T-000）
  - 地図はfolium + streamlit-foliumを使う方針を人間が承認済み
  - T-001の結果: 合計160件（当初161件と書いたのは誤り）。文字コードはUTF-8 BOM付き
  - 座標を使えない行が4件（`No.14` `No.15` `No.97`が空欄、`No.124`は経度が範囲外）。補正せず除外し、件数を画面に出す
  - 緯度の末尾にカンマが付く行が2件（`No.17` `No.157`）。カンマと空白を除去して使う
  - `富士河口湖`と`富士河口湖町`の表記ゆれあり。末尾の「市・町・村」の有無だけを統合する方針を人間が承認済み
  - `目撃時のクマ`が空欄の行が3件。吹き出しには「記載なし」と出す
  - T-002の結果: `tests/data/kuma_part1.csv`（5件）と`kuma_part2.csv`（3件）を作成。列は実データと完全一致（17列）。結合8件
  - 確認用CSVの中身は架空。実データではないため、画面や説明で実データとして扱わない
  - 列欠けの検証は、既存テストの流儀に合わせて`tempfile`で行う（ファイルは作らない）
  - T-003の結果: `load_records`をクマデータ用に差し替え、`load_all_records`と`duplicate_numbers`を追加。テスト8件が通る
  - 必須列は8列ではなく9列だった（`No.` `年月日` `目撃市町村` `場所` `目撃時のクマ` `人身被害の有無` `注意事項` `緯度` `経度`）
  - `時間`は`DATA.md`で必須ではないが吹き出しに出す。値が空でも止めない
  - 緯度の末尾カンマは読み込みでは取り除かない。地図に出す直前のT-004で扱う
  - サンプル用の`available_categories`と`filter_records`はまだ残してある。T-004で置き換える
  - `app.py`はT-006まで使えない。T-003の時点では「データを読み込めませんでした」、T-004以降は起動時のimportで失敗する
  - T-004の結果: `parse_coordinate` `is_inside_yamanashi` `split_by_coordinates` `base_municipality` `available_municipalities` `filter_by_municipality` `municipality_label`を追加。サンプル用の`available_categories`と`filter_records`は削除。テスト21件が通る
  - 実データ160件のうち、地図に出せるのは156件、出せないのは4件（`No.14` `No.15` `No.97` `No.124`）
  - 市町村は表記ゆれの集約後25種。`富士河口湖町`は9件になる
  - T-005の結果: `.venv`が無かったので作成し、`requirements.txt`の3行をインストール。streamlit 1.61.1 / folium 0.20.0 / streamlit-folium 0.27.4
  - Pythonは3.14.0。3つとも問題なく動いた
  - `streamlit_folium`に`__version__`は無い。バージョンを見るときは`importlib.metadata.version`を使う
  - Pythonは`.venv\Scripts\python`を使う。素の`python`にはstreamlitが入っていない
  - T-000の一部完了: 人間が https://www.pref.yamanashi.jp/opendata/kiyaku.html を確認。山梨県オープンデータ利用規約はCC BY 4.0互換で、商用利用・編集加工とも可能
  - 規約は出典表示に加え、加工した場合は「加工して作成」の明記を求めている。このアプリは絞り込みと表記ゆれ集約を行うため加工にあたる
  - 個人情報の確認は未了。T-000は`blocked`のまま
  - T-006の結果: `app.py`を市町村プルダウン＋地図＋件数の画面へ全面的に書き換えた。出所文言と「位置は目安」の但し書きも先に入れてある
  - ポート8501は他で使用中だった。確認は8511で行った
  - T-007の結果: `date_label`と`sighting_details`を`data_utils.py`に追加し、`app.py`の`popup_html`で吹き出しを組み立てた。テスト27件が通る
  - 吹き出しは自由記述を含むため`html.escape`でエスケープしている。実データにHTMLとして意味を持つ文字は含まれていなかったが、将来のデータ更新に備えて残す
  - 実データ160件すべてが5項目を作れ、日付が読めない行はなかった
  - 作業中にPCが落ちたが、ファイルは無事だった。再開時にテストで確認済み
  - T-008の結果: `data_period`を追加し、収録期間・0件案内・座標なし注記・出所リンクを画面に入れた。テスト32件が通る
  - 収録期間はデータから計算する。データを入れ替えても文言が自動で追従する
  - 西桂町は目撃1件だがその1件が範囲外の座標。実データで0件の画面を確認できる貴重なケース
  - 規約に沿って「このアプリは山梨県が作成したものではありません」を出所文言に追加した
  - T-000の個人情報確認の材料を収集した。`場所`は地内・大字レベルで番地はほぼ無し（`城東1丁目`が1件）。氏名・電話番号・メールは無し。`No.152`だけが地内名・市道番号・自宅の方角を含み、世帯が絞られる可能性がある
  - T-009の結果: `README.md`を新しいMVPの説明へ書き換え、1分デモの台本を追加。テスト32件が`-v`で通る
  - デモ例の初稿に誤りがあった。状況が空欄なのは`北杜市`ではなく`山梨市 No.21`。実データで数え直して修正済み
  - デモで見せる市町村: `身延町`（20件）、`西桂町`（0件＋非表示1件）、`笛吹市`（8件＋非表示1件）、`山梨市`（記載なし）
  - 人間が画面を確認し、主要操作は問題なしと判断。あわせて2件の不具合が見つかった（T-010）
  - 不具合1: ピンのアイコンがリンク切れ。foliumの既定マーカーはLeafletの画像をCDNから読むが、`iconUrl`がHTMLに書き出されずCSSの場所から相対解決している。streamlit-foliumはiframe内に描くため解決に失敗する
  - 不具合1の対応: 画像を使わない`CircleMarker`に変更。地図タイル以外の外部画像参照がゼロになったことを確認
  - 不具合2: マウスホイール1回で数段ズームする。Leafletの既定`wheelPxPerZoomLevel`が60で、Windowsのホイール1ノッチ(100〜120px相当)が複数段に相当するため
  - 不具合2の対応: `wheel_px_per_zoom_level=480` `wheel_debounce_time=200` `zoom_snap=1` `zoom_delta=1`を指定。4つとも地図のオプションに渡ることを確認
  - 感度調整では不十分だった。人間の再確認で、まだ2段ズームすると報告あり
  - 原因: Leafletはホイールの累積量を`Math.ceil`で必ず1段以上に丸める。感度を下げても、スクロールが判定区間をまたげば2回発火して2段動く
  - 最終対応: `scroll_wheel_zoom=False`でホイールズームを無効化。拡大縮小は+/-ボタンとダブルクリックに寄せ、画面に案内文を追加
  - 副次効果: Streamlitでページをスクロールしたときに地図が反応する問題も消える
  - ホイールズームを戻したい場合は`app.py`の`scroll_wheel_zoom=False`を消す
  - UI改善の指示書`yamanashi_bear_map_ui_redesign.md`を受領。U-001〜U-015としてタスク化した
  - 指示書は文字化けした状態で届いた。CSS変数・レイアウト図・型定義などASCII部分から内容を読み取っている。マーカー形状（涙型ピンに足跡）と色の意味（危険度ではなく新しさ）の読み取りは、人間の確認待ち
  - 実現可能性を確認済み。CartoDB positronタイル、`MarkerCluster`、`DivIcon`＋SVG、`fit_bounds`はfolium同梱で、追加インストールは不要
  - `st.markdown`のCSSはiframe内のLeafletに届かない。PopupとズームボタンのCSSは地図オブジェクトのヘッダーへ注入する
  - 人間の判断: 期間フィルターは実装する（`PROJECT.md`を更新済み）。経過日数の基準日はデータ内の最新日。配色はLight/Nature
  - T-010は人間が画面で再確認し`done`。ホイールズームの無効化とSVG以外のマーカー表示に問題なし
  - U-001の結果: `ui_styles.py`を新設し、色・radius・space・shadowをPython定数から組み立てる形にした。テスト41件が通る
  - 色をPython側に持つ理由: U-011のマーカーの色とサイドバーの凡例の色が食い違わないようにするため。CSSに直書きしない
  - 1440px幅での地図の横幅の見込みは1092px（75.8%）。sidebar 300px、左右padding 24pxずつで計算
  - `[data-testid="stHeader"]`は消さない。消すとサイドバーの開閉ボタンが無くなる。背景だけ透明にした
  - U-002の結果: `st.title`と常時表示の説明文を廃止し、ヘッダー1行に置き換えた。説明は「この地図について」のポップオーバーへ移した。テスト47件が通る
  - 足跡はSVGで描き、色は`fill: var(--brand)`でトークンから取る。SVG側に色を直接書かない
  - 更新日はデータ内の最新日（収録期間の最終日）を出す。取得日ではない
  - ヘッダーはデータを読んだ後でないと更新日が分からないため、`st.container()`で場所だけ先に取り、後から描いている
  - `st.popover`は`use_container_width`ではなく新しい`width="stretch"`を使う
  - U-003の結果: `municipality_counts`を追加し、プルダウンに件数を併記。件数と収録期間をサイドバーへ移し、黄色の警告を注記へ降格した。テスト58件が通る
  - プルダウンの件数は目撃の総数（地図に出せないものを含む）。サイドバーの大きな数字は地図に表示中の件数。数が食い違うため、注記で説明を添えている
  - 例: 西桂町はプルダウン1件、地図に表示中0件、注記「1件は地図に出していません」
  - プルダウンは`format_func`で表示だけ変える。選ばれる値は市町村名のままなので絞り込み処理は変更不要
  - セレクトの`min-height`を44pxにした（指示書§18のタップ領域）
  - U-004の結果: タイルを`CartoDB positron`へ変更。`tests/test_map.py`を新設し、テスト62件が通る
  - タイルURLは`https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png`
  - attributionは`© OpenStreetMap contributors © CARTO`が自動で付く。消していない
  - `tests/test_map.py`に「ホイールズームが無効のまま」を確認するテストを置いた。UI改善でT-010の修正を戻さないための番人
  - 依存が増えていないことをテストで確認する（`requirements.txt`が3行のまま）
  - U-005の結果: `bear_marker_svg`を追加し、`CircleMarker`を`DivIcon`のSVGピンへ置き換えた。テスト73件が通る
  - 地図の中へCSSを入れる仕組みを作った: `bear_map.get_root().header.add_child(folium.Element(MAP_STYLES))`。U-007のPopupとズームボタンも同じ仕組みを使う
  - `class_name`を渡すとLeaflet既定の`leaflet-div-icon`が置き換わるため、白い枠は出ない。生成HTMLで確認済み
  - ピンの色は今はブランド色。U-011で経過日数ごとの色に差し替える。`bear_marker_svg(color)`で色を渡せる形にしてある
  - hoverの拡大は`transform: scale(1.11)`（36px→約40px）。`prefers-reduced-motion`にも対応
  - U-006の結果: `MarkerCluster`を導入し、`icon_create_function`でクラスタの見た目を自前にした。テスト80件が通る
  - Leaflet既定のクラスタは件数が多いほど緑→黄→赤になる。これは「多い＝危険」に見えるため使わない。色は1色（ブランド色）で、件数では大きさだけを変える
  - 区分は指示書のとおり small 9件まで38px、medium 49件まで46px、large それ以上54px
  - ホバーで範囲の多角形を出す既定の動きは切った（`showCoverageOnHover: false`）
  - `leaflet.markercluster`のJSとCSSはCDNから読む。Python側の依存は増えていない
  - 実データ156件で確認。市町村単位では最大でも身延町20件なので、実際に出るのはsmallとmediumが中心
  - U-007の結果: `popup_card_html`を追加し、Popup・ズームボタン・attributionのCSSを上書きした。テスト89件が通る
  - カードの並びは日時・市町村・場所・状況・人身被害・出典。エスケープは`popup_card_html`の中で行う
  - 指示書§11は「データが存在する項目だけ表示する」だが、状況が空欄のときは`DATA.md`の決まりどおり「記載なし」を出す。人間が確定した`DATA.md`を優先した
  - 指示書§11にある「周辺を見る」ボタンは入れていない。必須5項目に含まれず、押した後の動きも決まっていないため
  - 閉じるボタンは指示書の32pxではなく44pxにした。§18のタップ領域44px以上を優先した
  - `.leaflet-popup-content`の`margin`を0にし、カード側の`padding`で余白を作る。閉じるボタンと日時が重ならないよう`__when`に右余白を入れている
  - U-008の結果: ブレークポイント別のCSSを追加し、地図の高さを500pxから620pxへ広げた。テスト95件が通る。これでP0が完了
  - 横スクロールは`overflow-x: hidden`で隠さず、原因側を直した。flexの子に`min-width: 0`、長い語に`overflow-wrap: anywhere`
  - 地図の高さは`st_folium`がピクセル指定しか受け付けないため固定値。CSSでviewport全高に伸ばすとLeafletが大きさを測り直せずタイルが欠けるので行わない
  - テストの書き方で2回失敗した。CSSコメントに`overflow-x: hidden`の文字列を書いていた点と、`.note`をグループ化セレクタに入れて先頭一致が変わった点。どちらもテストではなく書き方の問題だったので、CSS側の表現を直した
  - U-009の結果: `record_date` `reference_date` `days_ago` `sighting_tone` `days_ago_label` `season_start` `filter_by_period`を追加。テスト114件が通る
  - 基準日は2026年8月5日（データ内でいちばん新しい目撃日）
  - 期間の件数: 直近7日11件、直近30日49件、今季160件、すべて160件
  - 「今季」は基準日が属する年度（4月1日から）と定義した。今のデータは全件が令和8年度なので「すべて」と同数になる
  - 色の分布: recent 8件、mid 16件、older 26件、oldest 110件。4色とも実際に出る
  - 「直近N日」は基準日を1日目として数えたN日分（経過日数0〜N-1）
  - 日付が読めない目撃は、期間を指定したときは外す。置く場所を決められないため。「すべて」のときだけ残す
  - `date_label`と`data_period`にあった日付解釈の重複を`record_date`へまとめた
  - U-010の結果: サイドバーに期間のラジオを追加し、件数・プルダウン・地図を連動させた。テスト117件が通る
  - 初期値は「今季」。開いた直後に絞られていると、記録がないのか絞られているのか分からなくなるため
  - プルダウンに並べる市町村は、期間を変えても25件のまま増減させない。選んでいた市町村が消えて選択が戻るのを避けるため。件数だけが期間に連動する
  - 期間ごとの地図の件数: 直近7日11件、直近30日48件、今季156件、すべて156件
  - 件数表示に選んでいる期間を併記した。数字だけではどの条件の件数か分からないため
  - 0件の案内に期間と市町村の両方を入れた
  - Windowsの`strftime`は`%-m`を受け付けない。日付の組み立ては手で行う
  - U-011の結果: ピンを経過日数の4色に分け、サイドバーへ凡例、ツールチップと吹き出しへ「N日前」を入れた。テスト125件が通る
  - 凡例の文言は`tone_ranges()`がしきい値から組み立てる。`TONE_THRESHOLDS`を変えれば凡例も一緒に変わる
  - 色の意味を3か所で言葉にした: 凡例（0〜3日前など）、ツールチップ（1日前）、吹き出しのチップ（2日前）。色が読めなくても分かる
  - 凡例に「危険度ではありません」を明記した
  - クラスタの色はブランド色のまま。複数の経過が混ざるため、1つの色に決められない
  - 今季での内訳は recent 8件、mid 16件、older 25件、oldest 107件
  - 確認用スクリプトで`days_ago(...) or 9999`と書いたため、経過0日が最古として並んだ。アプリ側に同じ書き方がないことをgrepで確認済み
  - U-012の結果: ズームボタンを右下へ移し、「山梨全体へ戻る」ボタンと絞り込み状態のチップを地図上に置いた。テスト132件が通る
  - チップに「×」は付けない。地図はiframeの中にあり、そこからStreamlitの選択状態を変えられないため。押せないボタンを見せない
  - 代わりにサイドバーへ「絞り込みを解除」ボタンを置いた。初期状態のときは押せない状態にしてある
  - 解除は`on_click`のコールバックで行う。画面を描いた後にウィジェットの値を書き換えることはできないため
  - 埋め込むJavaScriptは、foliumが地図を作るscriptより前に出力される。そのままでは地図の変数が無くて落ちるので`DOMContentLoaded`まで待つ
  - `json.dumps`は既定で日本語を`\uXXXX`にする。`ensure_ascii=False`を付ける
  - 市町村名などの文字列は必ず`json.dumps`でJavaScript文字列にする。引用符でスクリプトが壊れるため
  - U-013の結果: `coordinate_bounds`を追加し、`map_view`の「平均中心と固定ズーム11」を`fit_bounds`へ置き換えた。テスト140件が通る
  - 目撃1件の市町村だと表示範囲が点になり、上限がないと限界まで拡大する。`max_zoom=13`と`padding=(30, 30)`で抑えた
  - 「すべての市町村」のときは`fit_bounds`を呼ばない。県全体の表示のままにする
  - `FIT_MAX_ZOOM`と`FIT_PADDING`は`ui_styles.py`に置いた。テストが同じ値を参照できるようにするため。`app.py`はStreamlitスクリプトでimportできない
  - U-014の結果: コントラストを実測し、不足していた2か所を直した。タップ領域を44pxへ揃え、focus-visibleとreduced-motionをページ側にも入れた。`tests/test_accessibility.py`を新設。テスト152件が通る
  - 指示書§14のアンバー`#D99A3D`は、中の白い足跡との差が2.43しかなく§18のコントラスト要件を満たせなかった。色相を変えず明度だけ下げた`#BA7E24`（3.44）へ変更した。指示書の値からの意図的な変更
  - `--border`（#DDE2DB）は面との差が1.31。区切り線には使えるが操作部品の枠には足りない。枠用に`--border-strong`（#809378・3.30）を足した
  - 他の3色（recent / older / oldest）は基準を満たしていたので変更していない
  - タップ領域44px: 市町村セレクト、期間のラジオ、ズームボタン、「山梨全体へ戻る」、Popupの閉じるボタン
  - コントラストの計算式そのものが正しいかを、黒と白（21.0）で確かめるテストを置いた
  - U-015の結果: READMEを新しいUIに合わせて全面更新し、デモ台本を作り直した。テスト152件が通る。これでUI改善フェーズが完了
  - テストの内訳: `test_data_utils.py` 62件、`test_map.py` 44件、`test_ui_styles.py` 34件、`test_accessibility.py` 12件
  - READMEに「見た目を直すときの注意」を追加した。CSSの入れ先が2つあることを知らないと直せないため
  - デモ台本の件数は実データで数え直して裏を取った
  - 人間の画面確認で不具合を発見。期間のラジオが暗い背景に暗い文字で読めなかった（U-016）
  - 原因: Streamlitがダークテーマで動いていた。`ui_styles.py`は明るい背景を前提に文字色を`#17201A`で固定していたため、暗い背景に暗い文字が乗った
  - 対応1: `.streamlit/config.toml`で`base = "light"`とデザイントークンの色を指定し、テーマを固定した
  - 対応2: 文字色を指定している箇所（サイドバー、プルダウン、解除ボタン）に背景も対で指定した
  - 対応3: `.gitignore`が`.streamlit/`を丸ごと除外していた。秘密情報は除外したまま`config.toml`だけ追跡するよう変更した
  - 学び: 文字色だけを固定するとテーマ次第で読めなくなる。色を指定するなら背景と対にする
  - 目撃リストの依頼を受けた。地図に出ている目撃を、日付の新しい順で地図の上に一覧表示する
  - 人間の判断: 行をクリックしたら、その地点へ寄って吹き出しが開く（L-004）
  - リストを地図の中に置くと、行のクリックをiframe内のJavaScriptで処理できる。Streamlitの再実行を挟まないので即座に反応する
  - クラスタの中のピンはそのままでは開けない。`zoomToShowLayer`で展開してから開く必要がある
  - 地図の生成HTMLは現在約192KB。156行の追加で数十KB増える見込み。問題が出ればL-002の時点で報告する
  - L-001の結果: `sort_by_date_desc`を追加。テスト166件が通る
  - 同じ日付のときは`No.`の大きい順にした。順番を決めないと開くたびに並びが変わり、さっき見た行を見失う
  - 日付が読めない行は末尾へまとめる。実データには該当がないが、データ更新に備えた決まり
  - 元のリストは並べ替えず、新しいリストを返す
  - 実データでの並び: 先頭は2026/8/5（身延町 相又地内・都留市 大野地内）、末尾は2026/4/2（身延町 寺沢地内）
  - U-016は人間が画面で再確認し`done`。ダークテーマの文字が読めることを確認済み
  - L-002の結果: `sighting_list_html`と一覧のCSSを追加。`tests/test_sighting_list.py`を新設。テスト179件が通る
  - 件数は渡された行の数から数える。別々に受け取ると「156件」と出しながら50件しか並んでいない食い違いが起こりうるため
  - `data_index`は整数へ変換してから埋め込む。文字列のままだと属性を抜け出せてしまう。壊れた値はValueErrorで落ちることをテストで確認
  - 大きさの実測: 156行で62KB（1行あたり399バイト）。地図全体の生成HTMLが約192KBなので3割ほど増える。動作に支障が出る量ではない
  - `test_map.py`の「attributionを隠していない」テストが、一覧の折りたたみで使う`display: none`に反応して落ちた。attributionの規則だけを見る形に直した
  - L-003の結果: 一覧を地図の右上へ載せ、見出しで開閉できるようにした。テスト187件が通る
  - 遅延実行のひな形を`deferred_script`として共通化した。`js_text`も外へ出して両方の関数から使う
  - `disableClickPropagation`と`disableScrollPropagation`を入れた。入れないと、行を押したつもりで地図が動き、一覧を送ったつもりで地図がずれる
  - `app.py`で`results`を`sort_by_date_desc`で並べ替えるようにした。ピンの追加順と一覧の順が同じになるので、行の`data-index`がそのままピンの番号になる（L-004で使う）
  - 実測: 一覧156行、ピン156個、生成HTMLは300KB（一覧なしのときは約192KB）
  - 確認スクリプトで`raw.count('sight-list__row')`と数えたら158になった。CSSの中の同じ文字列も数えていたため。属性で数え直して156を確認
  - L-004の結果: 行をクリックするとピンの吹き出しが開くようにした。テスト196件が通る
  - マーカーは変数そのものを参照する必要があるため、文字列にできない。`js_names`で変数名として成り立つかを確かめ、おかしな値はValueErrorで落とす
  - クラスタにまとまったピンはそのままでは開けない。`cluster.zoomToShowLayer(marker, callback)`で見えるところまで展開してから開く
  - 行の中の文字を押しても行を押したものとして扱うため、`event.target.closest`を使う
  - 開いた行に`aria-current`を付ける。どの行を見ているかが分かる
  - 行は`<button>`なので、キーボードのEnterとSpaceでも同じ動きになる
  - 生成HTMLは406KB（一覧のクリック処理を入れる前は300KB、一覧なしでは192KB）
  - 人間の画面確認で「一覧が見えない」と報告あり。調べたところ実装の不具合だった（L-007）
  - 原因: `streamlit-folium`は地図のHTMLをそのまま渡さず、地図オブジェクトの子要素をたどってJavaScriptを組み立て直す（`_generate_leaflet_string`）。`get_root().script`へ足したものは組み立ての対象外で画面に届かない
  - 同じ理由で、U-012のチップと「山梨全体へ戻る」もこれまで一度も表示されていなかった。ズームボタンの右下移動はfoliumの引数なので効いている
  - `header`へ入れたCSSは届く。`_get_header`が`root.header`を送るため
  - 私の確認方法の誤り: `_repr_html_()`をファイルに書き出して検証していた。そこには入っているのでテストも確認スクリプトも通っていたが、`st_folium`が送るものとは別物だった
  - マーカーの変数名は`streamlit-folium`が書き換える（実測: `marker_div_1_0`が別名になる）。名前を埋め込む方式そのものが使えない
  - 直し方: スクリプトを`folium.MacroElement`として地図の子要素にする。マーカーは`dataIndex`を持たせて`cluster.getLayers()`から探す。クラスタは`map.eachLayer`で見つける
  - テストの直し方: `st_folium`が送る文字列（`_get_header`と`generate_leaflet_string`の結果）に入っているかを確認する
  - L-007の結果: `map_overlays.py`を新設し、地図上の操作を`MacroElement`として作り直した。テスト204件が通る
  - 部品は`bear_map.add_child(...)`で地図の子要素にする。これで`_generate_leaflet_string`の対象に入る
  - 地図の変数は`{{ this._parent.get_name() }}`で参照する。名前を自分で組み立てない
  - マーカーは`dataIndex`という値で探す。`folium.Marker`のキーワード引数はLeafletのoptionsへ渡るので、`**{"dataIndex": index}`で持たせられる
  - クラスタは`map.eachLayer`で`zoomToShowLayer`を持つ層を探す。これも名前に頼らない
  - `MacroElement`の中身は素のJavaScript。`<script>`で包まない。`DOMContentLoaded`を待つ必要もない（地図の後に出力されるため）
  - `tests/test_streamlit_render.py`を新設。`_get_header`と`generate_leaflet_string`の結果に入っているかを見る。今後はこれを根拠にする
  - 実データでの確認: 一覧156行、ピン156個、送信量284KB。10項目すべて「届く」
  - `ui_styles.py`から`deferred_script` `js_names` `map_controls_js` `sighting_list_control_js`を削除した。届かない方式なので残さない
  - L-005の結果: 狭い画面での自動的な折りたたみ、一覧の高さ上限、focus-visible、選択中の行の印を追加。テスト213件が通る
  - 狭さの判定は`map.getSize().x`で行う。地図はiframeの中にあるので、ページ全体の幅では合わない。取れないときは`window.innerWidth`に落とす
  - 閉じるときは`aria-expanded`も`false`にする。見た目だけ閉じると読み上げと食い違う
  - 一覧の高さは`min(380px, 58vh)`。`vh`はiframeの高さに対する割合なので、地図をはみ出さない
  - 選択中の行は背景色だけでなく、左の線と`aria-current`でも示す
  - 一覧の配色のコントラストをテストに追加（市町村名・日付・場所・ホバー中の行）
  - L-006の結果: READMEに一覧の説明を入れ、デモ台本を更新。テスト213件が通る。これで目撃リストが完成
  - テストの内訳: `test_data_utils.py` 70件、`test_map.py` 44件、`test_ui_styles.py` 34件、`test_sighting_list.py` 32件、`test_accessibility.py` 23件、`test_streamlit_render.py` 10件
  - READMEの「見た目を直すときの注意」を4項目に増やした。特に「地図に足すJavaScriptは地図の子要素にする」はL-007の失敗そのものなので、経緯ごと残した
  - ファイル案内に`map_overlays.py`と`.streamlit/config.toml`を追加した
  - 毎日の自動更新と公開について相談を受けた。人間の判断: Streamlit Community Cloud＋Actionsで進める。ただし**今回は公開しない**。公開できる状態までを準備する
  - GitHub Pagesは静的ファイルしか配信できないため、Streamlitのアプリはそのままでは載らない。folium部分は静的HTMLにしても動くが、期間・市町村の絞り込みと出所表示はStreamlitのウィジェットなので作り直しになる
  - このフォルダはまだGitリポジトリではない（`git init`もリモートも未設定）
  - `app.py`が`int | None`を使っておりPython 3.10以上を要求する。他の3ファイルにある`from __future__ import annotations`が`app.py`にはない。G-002で足す
  - 取得日「2026年8月7日」と件数「160件」が`app.py`と`DATA.md`にベタ書き。自動更新すると表示と実態がずれる
  - `PROJECT.md`の「インターネットへの公開」は「作らないもの」に残したまま、準備までは行うと書き添えた
  - G-001の結果: `git init -b main`して2件のコミットを作った。25ファイルが追跡され、`.venv`と`__pycache__`は除外。リモートは設定していない
  - `core.autocrlf=true`で、県のCSVはCRLF。そのままだとGitが改行を変換し、Windowsでの作業内容とLinux（GitHub Actions）で取得した内容が食い違う。中身が同じでも毎日「変更あり」になり、毎日コミットされてしまう
  - `.gitattributes`に`*.csv -text`を書いて変換を止めた。`git add --renormalize`で入れ直し、保存されたCSVが元のファイルとバイト単位で一致することを確認済み
  - この対策がないと、G-004の「中身が変わったときだけコミットする」が成立しない
  - G-002の結果: 取得日を`data/fetched_at.txt`に記録し、そこから画面に出すようにした。`app.py`に`from __future__ import annotations`を足した。テスト220件が通る
  - ファイルの更新日時は取得日として使えない。`git clone`するとクローン時刻になるため、Streamlit Community Cloudでは常に「今日取得した」と出てしまう。取得した日そのものを書いたファイルを読む
  - 読めないときは日付を出さず「取得日不明」と表示する。分からないのに日付を出すと、いつのデータかを誤って伝えることになる
  - ファイルが無くてもアプリは止まらない
  - 件数と収録期間は元から計算していたので変更不要だった。画面のベタ書きは取得日1か所だけ
  - `japanese_date`を共通化し、収録期間・取得日・注記で書き方をそろえた
  - `DATA.md`と`README.md`には取得日「2026年8月7日」と件数「160件」が残っている。文書の更新はG-007で行う
  - G-003の結果: `scripts/fetch_data.py`を作り、実際に県のサイトから取得できた。テスト236件が通る
  - 取得は標準ライブラリ（`urllib`）で行う。`requirements.txt`は増やさない
  - 差し替える前に確かめる: 空でないか、UTF-8で読めるか、必須列がそろっているか、目撃が1件以上あるか、地図に出せる座標が1件以上あるか
  - 2つとも確認できてから差し替える。片方だけ新しい状態にしない
  - 別名で書いてから置き換える。途中で止まっても壊れたファイルが残らない
  - **実行したら実際にデータが新しくなった**（160件→163件、8月6日と7日の目撃が追加）。取得日は2026-08-08になった
  - **重大な発見**: 実データに対するテストが件数や日付をベタ書きしていたため、データが新しくなった瞬間に7件が落ちた。自動更新では「データが正しく更新された日ほどテストが落ちる」ことになる
  - 対応: 実データのテストを「常に成り立つ性質」に書き換えた。件数は2ファイルの合計と一致するか、地図に出せない行は本当に座標が使えないか、基準日は最新の目撃日と一致するか、など
  - 市町村名の決め打ち（身延町・西桂町）もやめた。件数が最多の市町村を選ぶ、条件に合う市町村を探す、という形にした
  - 古いデータ（160件）と新しいデータ（163件）の両方で236件が通ることを確認済み
  - データの構造は変わっていない（17列・`No.`の重複なし・26市町村・座標が使えない行は同じ5件）。`DATA.md`の列説明と処理はそのままでよい
  - `DATA.md`と`README.md`の件数「160件」と取得日はずれた。G-007で直す
  - 未決定: 取得日を毎回書き直すか、データが変わったときだけ書き直すか。毎回だとActionsが毎日コミットする（リポジトリの活動が続き、60日で止まる問題は避けられる）。G-004で決める
  - G-004の結果: `.github/workflows/update-data.yml`を置いた。`tests/test_workflow.py`で内容を確認する。テスト250件が通る
  - 保留していた判断を決めた: **取得日は毎回書き直す**。データが変わらない日もコミットが入る
  - 理由: クマの目撃は冬季に途絶えることがある。データが変わった日だけコミットする方式だと、目撃が増えない日が60日続いた時点でGitHubが定期実行を自動で止め、再開に人の操作が要る。履歴のきれいさより、止まらないことを優先した
  - 実行の順番は 取得 → 依存のインストール → テスト → コミット。テストが落ちたらコミットしない。おかしなデータをそのまま画面へ出さないため
  - 取得は標準ライブラリだけで動くので、依存のインストールより先に行う。取れなかった日は早く止まる
  - `TZ: Asia/Tokyo`を指定した。runnerの既定はUTCで、日本時間の朝6時に動かすと前日の日付が記録されてしまう
  - `git add data`だけを行う。取得と関係ないファイルを巻き込まない
  - コミット判定（`git add data`と`git diff --cached --quiet`）を、変更ありの場合と変更なしの場合の両方で手元で確かめた
  - PyYAMLが入っていないためYAMLとしての妥当性は確認できていない。アプリの依存を増やさない方針なので追加しなかった。正しさはGitHubへ置いて実行するまで確定しない
  - push先のブランチ保護や既定ブランチ名によってはpushが失敗する。G-007で対処方法を書く
  - G-007の結果: READMEに「データを取り直す」「毎日の自動更新」「公開するときの手順」「公開をやめるときの手順」を追加。`DATA.md`の件数と取得日を直した。テスト250件が通る。これで公開の準備が完了
  - 文書に件数や日付を書き写さない方針にした。毎日変わるため、書いた瞬間から古くなる。件数は画面のサイドバーで確認する
  - `DATA.md`の「実データで確認した内容」は、件数ではなく「壊れ方の種類が増えていないか」を見るための記録だと書き添えた
  - READMEの「公開をやめるときの手順」に、Actionsを止めることを書いた。止めないとアプリを消したあとも毎日県のサイトへアクセスし続ける
  - pushやコミットが失敗するときの対処（既定ブランチ、ブランチ保護、Workflow permissions）も書いた
  - `DATA.md`第7節の「テストで使える小さなデータを用意した」がT-002で済んでいたのに未チェックのままだった。あわせて直した
  - 公開時の注意点について相談を受けた。特商法は無料・販売なしのため該当しない見込み。ライセンス面（出典・加工の明記・県が作成したものでない旨）は対応済み
  - 不足しているのは免責・プライバシー・連絡先の3点。P-001〜P-004として起こした
  - 実測: ページを開くだけで5つの外部ホストへ通信する（cdn.jsdelivr.net、cdnjs.cloudflare.com、code.jquery.com、netdna.bootstrapcdn.com、basemaps.cartocdn.com）。利用者のIPアドレスがこれらに渡る。foliumが自動で読み込むもので、減らすのは容易ではない
  - 人間の判断: 連絡先はGitHubのIssuesへ誘導する。リポジトリが未作成のためURLは未確定。設定できる形にして、公開手順に「URLを入れる」を足す
  - 置き場所はこちらで決めた: 免責の要点は画面下（開かずに読める）、詳しい内容とプライバシー・連絡先は「この地図について」
  - 県の公開状況を調べた結果も判断材料になる: `No.152`の記述は県のPDFにも載っており、切れているのは市道番号の部分だけ。座標はPDFになくCSVにある。県自身も出没マップを公開している
  - P-001の結果: `disclaimer_html`を追加し、画面下に免責を常時表示にした。テスト261件が通る
  - 散らばっていた注意書き（位置は目安、状況欄の扱い）を免責へまとめた。関係する注意を離して置くと、片方だけ読まれる
  - 4項目にした: 位置の精度、更新の遅れと公式発表の確認、責任を負わないこと、状況欄を個人特定に使わないこと
  - 警告色は使わない。読み飛ばされない程度に背景を変えるだけにした
  - **ズームボタンの案内が「左上」のままだった**（U-012で右下へ移した）。あわせて直した。テストで文言を固定した
  - 免責がポップオーバーより前にあること、出所表示より上にあることをテストで確かめている
  - P-002の結果: 「この地図について」に免責の詳しい版・プライバシー・連絡先を足した。テスト276件が通る
  - 外部通信先は`EXTERNAL_HOSTS`として1か所に置いた。P-003で実際の生成物と突き合わせる
  - プライバシーには「集めていないこと」と「外部へ通信していること」の両方を書く。前者だけだと後者を隠すことになる
  - 通信先は名前で書く。「外部へ送信します」だけでは、どこへ渡るのかが分からない
  - 連絡先URLは`CONTACT_URL`の1か所で設定できる。空のときは「公開時に設定します（現在は未設定）」と出す。URLが無いのに連絡先があるように見せない
  - 元データの誤りは県でないと直せないため、案内先を「このアプリの連絡先」と「県の担当課」に分けた
  - **「取得した時点のデータで、自動更新はしません」が実態と違っていた**（毎日の更新を用意した）。削除し、テストで再発を防いだ
  - P-003の結果: `tests/test_external_hosts.py`を新設。画面に書いた通信先と実際が一致することを両方向で確かめる。テスト283件が通る
  - 調査の途中で誤った結論を出しかけた。`streamlit_folium._get_header`は`<script src>`と`<link>`を取り除くため、ヘッダーだけを見ると「通信先は地図タイクだけ」に見える
  - 実際は`css_links`/`js_links`という別の引数でフロントエンドへ渡され、ブラウザが読み込む。**画面に書いた5社は正しかった**
  - 集め方は`streamlit_folium`と同じにした（地図を再帰的にたどり`default_css`/`default_js`を集める）
  - `streamlit_folium`側の作りが変わったら気づけるよう、`css_links=css_links`などの記述が残っているかを見るテストも置いた
  - 番人が本当に働くかを確認した。使っていない相手を足すと2件落ち、地図タイルの記載を消すと2件落ちる
  - P-004の結果: READMEの公開手順に連絡先URLの設定を足した。テスト283件が通る。これで免責・プライバシー・連絡先が完了
  - 公開手順を5段階から6段階にした。`git push`の直後に「連絡先を設定する」を入れ、デプロイ前に必ず通るようにした
  - 公開後の確認に「連絡先が『現在は未設定』になっていないこと」を入れた
  - READMEに「画面に出している注意書き」の節を追加。どこに何が出ているかの一覧
  - 「公開する前に決めること」に、削除依頼が来たときの選択肢を足した。除外機能はなく、県への案内か一時停止のどちらか
  - 県が同じ記述をPDFで公開していること、県のPDFでは長い記述が切れていることも書き添えた
  - READMEのテスト件数（283件）が実際と一致することを確認した
  - 人間が公開してよいと判断したため、G-005を実行した。`PROJECT.md`の「作らないもの」から公開を外し、完成条件へ移した
  - **push先が2つの候補になった。** 作業前から`origin`が`shingen-py/sample_20260808`を指しており、すでにpublicで3コミット入っていた。この場に記録のない操作が行われていた
  - 人間の判断で`shingen-py/sample_20260808`を使う。連絡先URLもこのリポジトリのIssuesにした
  - 指示に従って`hroabe/yamanashi-bear-map`（public・空）を先に作ってしまった。使わないので削除するかどうかは人間の判断待ち
  - **YAMLは正しかった。** ワークフローが登録され、名前も読めている。PyYAMLで検証できていなかった点は解消
  - ワークフローの初期状態が`disabled_manually`だった。`gh workflow enable`で有効にした
  - **組織の設定で「ワークフローの書き込み権限」が禁止されている**（リポジトリ既定はread）。それでもワークフロー側の`permissions: contents: write`は有効だった。実際にpushが通ることを確認済み
  - 1回目の手動実行は「変更なし」で早期終了し、pushの経路を通らなかった。取得日を1日戻してから再実行し、コミットとpushが通ることを確かめた（`0a4a6b2..5f1a026`）
  - runnerの時刻は`TZ: Asia/Tokyo`が効いている。UTC 21:19の実行で取得日が2026-08-08と記録された
- 次の1手: 人間がStreamlit Community Cloudへデプロイする（G-006）

## `blocked`にするとき

```text
止まった場所:
表示されたエラー:
試したこと:
人間に決めてほしいこと、または次に試すこと:
```

## 完了チェック

- [ ] `doing`が残っていない
- [ ] 主要操作を最初から最後まで試した
- [ ] 自動テストの結果を確認した
- [ ] サンプルまたはデータの出所が画面に表示される
- [ ] 未完成の項目は`todo`または`blocked`として残した
