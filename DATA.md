# DATA.md — データの説明書

この文書は、あなたのMVPが何を事実として使うかを説明します。`PROJECT.md`を確定した後、`PROMPTS.md`の「2. DATA.mdを一緒に決める」を使って埋めます。

実データの利用条件と出所は、AI任せにせず人間が元サイトで確認します。

## 1. 使用するデータ

- データの状態: ダウンロード済み。`data/`に配置。取得日は`data/fetched_at.txt`にある
- データ名: ツキノワグマ出没・目撃情報（令和8年度）／ツキノワグマ出没・目撃情報（直近1か月）
- サンプル / 実データ: 実データ
- 提供者: 山梨県 森林環境部 自然共生推進課
- 取得元URL:
  - 令和8年度: https://catalog.dataplatform-yamanashi.jp/dataset/kuma1
  - 直近1か月: https://catalog.dataplatform-yamanashi.jp/dataset/kuma0
  - 関連（県ホームページ）: https://www.pref.yamanashi.jp/shizen/kuma2.html
- 利用条件・ライセンス: 山梨県オープンデータ利用規約（https://www.pref.yamanashi.jp/opendata/kiyaku.html ）。CC BY 4.0と互換で、商用利用・編集加工とも可能。**2026年8月7日に人間が確認済み**
  - 出典の表示が必要。加工して使う場合は「加工して作成」した旨も併せて記載する
  - データ提供団体が作成したかのような見せ方をしてはいけない
  - 県は完全性・正確性を保証しない
- 取得日: `data/fetched_at.txt`に記録する。画面の出所表示はこの値を使う。ここに日付を書き写さない（書き写すとすぐ古くなる）
- こちらが取り直す頻度: 1日1回。`scripts/fetch_data.py`を`.github/workflows/update-data.yml`から動かす
- 提供元が更新する頻度: **要確認**。カタログのメタデータ最終更新は2026-08-06。県はX（旧Twitter）で速報を配信している
- 画面に表示する出所文言:

```text
出典：山梨県 森林環境部 自然共生推進課『ツキノワグマ出没・目撃情報（令和8年度／直近1か月）』
（やまなしデータプラットフォーム・◯年◯月◯日取得）を加工して作成
利用条件：山梨県オープンデータ利用規約（CC BY 4.0互換）
地図上の位置は目撃地点のおおよその目安です
```

このアプリは市町村での絞り込みと表記ゆれの集約を行うため、規約上の「編集・加工」にあたる。そのため「を加工して作成」を必ず入れる。

## 2. アプリで読むファイル

- ファイル名: `data/2026kumadata.csv`（令和8年度）と `data/2026kumadata_new.csv`（直近1か月）
- ファイル形式: CSV
- 文字コード: UTF-8（BOM付き）。`utf-8-sig`で読む
- 使う件数: 2ファイルを結合したすべて
- 取得日を記録するファイル: `data/fetched_at.txt`（`YYYY-MM-DD`の1行）
- アプリが実行時に読む先: 公開リポジトリの`data/`を`raw.githubusercontent.com`から直接読む（`remote_data.py`）。読めないときは同梱の`data/`へ戻る
- 読みに行く間隔: 1時間（`remote_data.TTL_SECONDS`）。データは1日1回しか変わらないため

同梱の`data/`は「読めなかったときの控え」である。この区別がないと、同梱ファイルの日付を見て「画面もこの日付のはず」と誤読する。

**件数はここに書かない。** 毎日取り直すため、書いた瞬間から古くなる。件数は画面のサイドバーで確認する。

年度ファイルは「当年度（直近1か月を除く）」、直近ファイルは「当年度（直近1か月のみ）」で、期間は重ならない。`No.`は年度ファイルから直近ファイルへ連番で続き、重複はない。

参考として、2026年8月8日に取得した時点では112件と51件の計163件だった。

自動テスト用には、この実データとは別に5〜10件の小さなCSVを用意する。

## 3. 列の意味

両ファイルとも列構成は同一（17列）。

| 列名 | 必須 | 意味 | 例 | 画面で使う場所 |
|---|---|---|---|---|
| No. | はい | 通し番号 | `1` | 重複確認 |
| 年月日 | はい | 目撃日 | `2026/4/2` | 表示 |
| 目撃年月日 | いいえ | 同じ日付の別表記 | `2026年4月2日.` | 使わない |
| 時間 | いいえ | 目撃時刻 | `14:00` | 表示 |
| 目撃市町村 | はい | 目撃があった市町村名 | `身延町` | 絞り込み |
| 場所 | はい | 地内名などの場所 | `寺沢地内` | 表示 |
| 天候 | いいえ | 天候 | `晴れ` | 使わない |
| 目撃時のクマ | はい | そのときの状況の説明文 | `沢にいた。避難したためその後は不明` | 表示 |
| 目撃時の目撃者の行動 | いいえ | 目撃者の属性・行動 | `近隣住民` | 使わない |
| 目撃した環境 | いいえ | 目撃地点の環境 | `集落付近` | 使わない |
| その後の対応 | いいえ | 自治体などの対応 | `パトロール実施` | 使わない |
| 人身被害の有無 | はい | 人身被害の有無 | `無し` | 表示 |
| 推定年齢 | いいえ | クマの推定年齢 | `コドモ` | 使わない |
| 目撃頭数 | いいえ | 目撃した頭数 | `1` | 使わない |
| 注意事項 | はい | 全行同一の但し書き | `次の緯度・経度は、目撃地点の大まかな付近を表示した目安です。idはデータ処理上の数字です。` | 但し書きの出典 |
| 緯度 | はい | 十進度の緯度 | `35.4703681` | 地図のピン |
| 経度 | はい | 十進度の経度 | `138.4374339` | 地図のピン |

`注意事項` 列にあるとおり、緯度・経度は目撃地点そのものではなく、おおよその付近を示す目安である。画面でも同じ意味の但し書きを必ず表示する。

## 4. アプリが行う処理

0. （毎日・GitHub Actions上で）`scripts/fetch_data.py`が県のサイトから2つのCSVを取り直す。読めるかを確かめてから差し替え、取得日を`data/fetched_at.txt`へ書く。おかしなデータのときは差し替えず、今あるファイルを残す
0-2. （画面を開いたとき）`remote_data.py`が公開リポジトリの`data/`から2つのCSVと`fetched_at.txt`を読む。取得日を先に読み、次にCSVを読む。読んでいる間に毎朝の更新が入っても、取得日が実際より新しくなる側へは転ばないようにするため。手順0と同じ基準で確かめ、1つでも欠ければ**すべて捨てて**同梱のファイルへ戻り、その旨を画面に出す。データと取得日は必ず同じ元から取る（混ぜない）
1. 2つのCSVを読み込み、縦に結合する（列構成は同一）
2. `緯度` `経度` の前後の空白とカンマを取り除いてから数値へ変換し、変換できない行と範囲外の行を地図表示から除外して件数を数える
3. `目撃市町村` の一覧を作り、末尾の「市・町・村」の有無で表記を集約して、プルダウンの選択肢にする
4. 選ばれた市町村で絞り込む
5. 絞り込み結果を地図にピン表示し、件数を数字で出す
6. ピンの吹き出しに `年月日` `時間` `場所` `目撃時のクマ` `人身被害の有無` を出す
7. 画面に出所文言と「位置は目安」の但し書きを常時表示する

元データを書き換えるか: 書き換えない

## 5. データ品質とエラー時の扱い

- 必須列がない場合: 不足している列名を画面に出して停止する。推測で補わない
- 緯度・経度の書式が乱れている場合: 前後の空白とカンマを取り除いてから数値へ変換する。値そのものは変えない
- 緯度・経度が空欄、数値にできない、または範囲外の場合: 地図から除外し、「座標なしN件は地図に表示できません」と画面に出す。範囲は緯度35.0〜36.5、経度138.0〜139.5とする
- `目撃市町村` が空欄の場合: 「市町村不明」として扱い、プルダウンの選択肢には出さない。件数には数える
- `No.` が重複している場合: どちらも消さずに両方表示する。重複を検知できるテストを置く
- 日付の形式が違う場合: `年月日` を `YYYY/M/D` として解釈する。解釈できない行もピンは表示し、日付欄は「日付不明」と出す
- 表記揺れがある場合: `目撃市町村` は前後の空白を除去したうえで、末尾の「市・町・村」の有無だけをゆれとみなし、付いている表記へ寄せて集約する。それ以外の補正は行わない
- `目撃時のクマ`（状況）が空欄の場合: 吹き出しに「記載なし」と表示する
- `人身被害の有無` に「無し」と「無」の表記ゆれがある: 表示のみに使うため加工しない
- 0件になった場合: 「選んだ市町村では、収録期間（データから計算した期間）に目撃の記録がありません」と収録期間を添えて表示する
- リポジトリから読めない、または読めた中身がおかしい場合: 同梱のデータへ戻す。取得日も同梱のものを使い、データと取得日をちぐはぐにしない。画面に「最新のデータを読み込めなかったため、アプリに同梱されたデータ（◯年◯月◯日取得）を表示しています」と出す。**黙って古いデータを出さない。**最新だと思って読まれてしまうため

AIが勝手に値を作って補わないよう、エラーにするものと無視するものを決めます。

### 2026年8月8日時点の実データで確認した内容

毎日取り直すため、下の件数はこの日の値。**構造（列と壊れ方の種類）は変わっていない。**
データを取り直したあとにここを見直すときは、件数ではなく「壊れ方の種類が増えていないか」を見る。

- 合計163件（112件＋51件）。`No.`の重複なし
- 座標を使えない行は4件
  - `No.14`（上野原市 秋山地内）、`No.15`（南アルプス市 上市之瀬地内）、`No.97`（笛吹市 芦川町上芦川地内）: 緯度・経度が空欄
  - `No.124`（西桂町 下暮地地内）: 経度が`38.8243704`で範囲外。`138.8243704`の誤りに見えるが、データにない値を作らないため補正せず除外する
- 緯度の末尾にカンマが付く行が2件（`No.17`、`No.157`）。カンマと空白を取り除けば有効な数値なので、地図に表示する
- `目撃市町村`の表記ゆれは1件（`富士河口湖`→`富士河口湖町`へ集約）
- `目撃時のクマ`が空欄の行が3件（`No.21`、`No.97`、`No.127`）
- `注意事項`は全行で同一の文言

## 6. 安全と利用条件

- [x] 誰が提供したデータか分かる（山梨県 森林環境部 自然共生推進課）
- [x] 利用・加工・再配布の条件を人間が確認した（2026年8月7日・山梨県オープンデータ利用規約／CC BY 4.0互換）
- [x] 画面に表示する出典名とURLを決めた
- [x] 個人情報や秘密情報が含まれていない（2026年8月7日・人間が確認）
  - 氏名、電話番号、メールアドレスは含まれない
  - `場所`は「〜地内」など大字・地内レベル。番地は`城東1丁目`の1件のみ
  - 学校・病院などの施設名は含まれない
  - 目撃者を表す`目撃時の目撃者の行動`列は画面に出さない
  - `No.152`のみ、地内名・市道番号・自宅の方角が揃い世帯が絞られる可能性がある。県が公開している記述をそのまま表示する方針とし、画面に注意書きを出す
- [x] APIキー、パスワード、非公開URLを含まない
- [x] データから言えないことを推測で補わない（危険度の判定はしない。位置は目安と明示する）

## 7. 実装前の確認

- [x] `PROJECT.md`の主要操作に必要な列がある（`目撃市町村` `緯度` `経度`）
- [x] 必須列と文字コードを確認した（17列・UTF-8 BOM付き）
- [x] 欠損、重複、表記揺れを数件目視した（上記「実データで確認した内容」）
- [x] サンプルと実データを区別して表示できる（出所文言を常時表示する）
- [x] テストで使える小さなデータを用意した（`tests/data/kuma_part1.csv`と`kuma_part2.csv`）

## 用途に対する制約

緯度・経度は「目撃地点の大まかな付近」の目安である。そのため、通学路の何メートル横で目撃されたか、という精度の判断には使えない。このMVPで分かるのは「その市町村・その地区で、いつ、どんな状況の目撃があったか」までとする。

## 付属サンプルの参考情報

まだ自分のデータを決めていない間は、動作確認用として`data/sample.csv`があります。

- 内容: 架空の立ち寄りスポット
- 出所表示: もくもく会サンプルデータ
- 実在性: すべて架空
- 主な列: `id`、`name`、`category`、`area`、`description`、`source_name`、`source_url`

この情報を、自分のMVPのデータとしてそのまま確定しないでください。
