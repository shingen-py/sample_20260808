# PROJECT.md — 何を作るか

この文書は、あなたが作るMVPの目的と完成条件を決めるための1枚です。付属サンプルの説明書ではありません。

`PROMPTS.md`の「0. MVPを一緒に決める」を使い、AIの質問に答えながら埋めます。AIが変更案を示した後、人間が確認してから反映します。

## MVPの6項目

- プロジェクト名: 山梨クマ目撃マップ（市町村別）
- 対象にする利用者: 山梨県在住の住民（子供の通学路が心配な保護者1人）
- 利用者が困る場面: 朝夕の送り迎えや散歩のルート上に、最近クマの目撃があったかを地図で確認する手段がない
- 使うデータ: 山梨県 森林環境部 自然共生推進課「ツキノワグマ出没・目撃情報」のCSV（令和8年度分と直近1か月分を結合して161件。緯度・経度あり。ライセンスと更新頻度は要確認。詳細は`DATA.md`）
- 1つの主要操作: 市町村を選ぶ
- 操作後に起きてほしいこと: 選んだ市町村の目撃地点が地図にピン表示され、件数が分かる。ピンを押すと日付・場所・状況が読める

## プロジェクトを1文で

`[対象の利用者]`が、`[データ]`を使って、`[1つの操作]`を行い、`[得られる結果]`を得られるアプリ。

記入した1文:

> 山梨県在住の保護者が、県内のクマ目撃データを使って、市町村を選ぶ操作を行い、周辺の目撃地点と件数・日付・状況を地図で確認できるアプリ。

## 今回の完成条件

自分のMVPに合わせて、画面またはテストで確認できる言葉へ書き換えます。

- [x] アプリが起動し、地図と市町村の選択欄が表示される
- [x] 市町村を選ぶと、その市町村の目撃地点だけが地図にピン表示される
- [x] 選んだ市町村の件数が数字で表示される
- [x] ピンを押すと、日付・場所・状況が読める
- [x] 目撃0件の市町村を選んだとき、「この期間の記録はありません」と次の行動が分かる表示になる
- [x] データの出所（提供者名とURL）またはサンプル表示が画面に見える
- [x] 市町村での絞り込み処理を自動テストで確認できる
- [x] 人間が画面を操作して確認している
- [ ] 1分以内でデモできる

UI改善フェーズで追加した完成条件:

- [x] 期間（7日／30日／今季／すべて）を選ぶと、件数と地図のピンが変わる
- [x] 地図が画面の主役になっている（1440px幅で横方向の70%以上を占める）
- [x] マーカーの色の意味が、凡例とツールチップの両方で分かる

目撃リストで追加した完成条件:

- [x] 地図の上に、表示中の目撃が日付の新しい順で一覧表示される
- [x] 一覧の行を押すと、その目撃地点の吹き出しが開く

公開で追加した完成条件:

- [x] データを取得し直すスクリプトがあり、壊れたデータでは差し替えない
- [x] 取得日・件数・収録期間が、データファイルから自動で決まる
- [x] 公開するときの手順と、やめるときの手順がREADMEに書かれている
- [x] 免責・プライバシー・連絡先が画面に出ている
- [x] GitHubのリポジトリがあり、毎日の更新が動く
- [x] 公開されたURLでアプリが開く

## 今回は作らないもの

3時間で主要操作を完成させるため、次は原則として後回しにします。

- ログイン、ユーザー登録
- お気に入りの永続保存
- 複数画面の複雑な操作
- データベース
- AI APIや外部サービスとの接続

自分のMVPで追加する「作らないもの」:

- 住所を入力して周辺N kmを表示する機能（住所から緯度経度への変換が必要）
- 利用者からの目撃情報の投稿・報告機能
- メール・LINE等への通知・アラート
- モバイル用のBottom Sheet、地図のviewport全高表示、Lighthouseの完全対応（Streamlitの構造上できない。代替はTASKS.mdに記載）
- URLへの絞り込み状態の保存、現在地周辺の表示、時系列スライダー、ヒートマップ、PWA
- 一覧の並び順の切り替え（古い順・市町村順など）。既定の日付降順だけにする
- 一覧のCSVダウンロード
- 地図を動かすと一覧が絞られる連動

期間での絞り込みと目撃の一覧表示は、当初「作らないもの」に入れていたが、依頼を受けて完成条件へ移した。

インターネットへの公開も、当初は「作らないもの」に入れていた。準備が整い、人間が公開してよいと判断したため完成条件へ移した（2026年8月8日）。
公開先はGitHub（shingen-py/sample_20260808・public）とStreamlit Community Cloud。

未確定（人間の判断待ち）:

- 危険度の自動判定・予測、全県の統計グラフ・ヒートマップは、今回の完成条件に入れていない。「作らないもの」へ明記するかは未決定
  - ただし、マーカーの色は危険度ではなく「目撃からの経過日数」を表す。危険度は算出しない

## 画面の流れ

```text
アプリを開く（山梨県全体の地図）
  ↓
市町村をプルダウンで選ぶ
  ↓
その市町村の目撃地点がピン表示され、件数が出る
  ↓
気になるピンを押し、日付・場所・状況を読んで外出ルートを判断する
```

## 付属サンプルについて

初期の`app.py`と`data/sample.csv`は、CSV検索が動くことを確認するための架空サンプルです。サンプルの「山梨おでかけ候補ファインダー」を、そのままあなたのMVPとして扱わないでください。

## 判断に迷ったときの優先順位

1. 1人の利用者が主要操作を最後まで行えるか
2. データの意味と出所が正しく伝わるか
3. エラー時に利用者が次の行動を選べるか
4. 見た目や追加機能
